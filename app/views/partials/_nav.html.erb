<div class="contain-to-grid">
  <div class="logo"><%= link_to image_tag('chess_store_title.png'), home_path %></div>
  <div class="login_img">
        <% if logged_in? %>
            <%= link_to 'Logout', logout_path %>
            <br><%= current_user.proper_name%>
        <% else %>
          <%= link_to image_tag('chess_login_2.png'), login_path %>
        <% end %>
        <%= link_to 'Cart', show_cart_path %>
  </div>
  <nav class="top-bar" data-topbar role="navigation"> 
    <section class="top-bar-section">
      <ul class="left">
        <!-- link to home -->
        <li><%= link_to "Home", home_path %></li>

        <!-- options for items -->
        <li class="has-dropdown">
          <%= link_to "Items", items_path %>
          <ul class="dropdown">
            <li><%= link_to "Item List", items_path %></li>
            <li><%= link_to "Reorder List", home_path %></li>
            <li><%= link_to "Add an Item", new_item_path %></li>
          </ul>
        </li>

        <!-- options for purchases -->
        <li>
          <%= link_to "Purchases", purchases_path %>
        </li>
               <!-- options for purchases -->
        <li>
          <%= link_to "Schools", schools_path %>
        </li>

        <!-- options for prices -->
        <li>
          <%= link_to "Prices", item_prices_path %>

        </li>
          <!-- Order History -->
        <li>
        <% if logged_in? %>
          <%= link_to "My Order History", order_history_path %>
        <% end %>
        </li>

      </ul>

    </section>
  </nav>
</div>